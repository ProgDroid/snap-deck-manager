[package]
name = "common"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "^1", features = ["derive"] }
serde_json = "^1"
serde-aux = "^4.5"
strum = { version = "^0.26", features = ["derive"] }
